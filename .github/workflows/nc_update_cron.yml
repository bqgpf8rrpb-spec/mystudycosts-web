name: NC Update Cron (Deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Write deprecation summary
        run: |
          echo "## Deprecated workflow" >> $GITHUB_STEP_SUMMARY
          echo "Use \`update-nc-data.yml\` as the single NC automation workflow." >> $GITHUB_STEP_SUMMARY

